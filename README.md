## University

Require the config file to run the app. 
The schema of config file:

<pre>
    <code>
{
    "db": {
        "host": "${host}",
        "port": "${port}",
        "username": "${username}",
        "password": "${password}",
        "database": "${database}"
    }
}
    </code>
</pre>